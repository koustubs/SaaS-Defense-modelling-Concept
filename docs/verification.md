# Verification Record

This record covers the reviewed repository state on 2026-08-22. It is a local verification record, not a penetration-test certificate or deployment approval.

## Clean setup

A new Python 3.13.12 virtual environment was created outside the repository. The documented editable installation completed from `pyproject.toml` with the `dev` extra, `pip check` reported no broken requirements, and the installed `threat-analyzer --version` command returned `1.1.0`.

Principal installed versions were FastAPI 0.141.1, Uvicorn 0.52.4, SQLAlchemy 2.0.52, Jinja2 3.1.6, argon2-cffi 25.1.0, cryptography 50.0.0, HTTPX 0.28.1, python-multipart 0.0.32, pytest 9.1.1, and Ruff 0.16.4. The temporary environment was removed after verification.

## Archive and evidence

- Recomputed `DFD.zip` SHA-256: `844167c9f3890357c4570fedfd2ff945666383867373975fd8c7945765dfbd0f`.
- The archive has 51 entries: 44 files and 7 directory records.
- `original_evidence/MANIFEST.json` accounts for all entries with 27 retained artifacts and 24 exclusions.
- Every manifest source size and hash matched the corresponding ZIP entry.
- Every retained artifact matched its source ZIP bytes exactly.
- All 27 `MANIFEST.sha256` entries were recomputed with no missing, extra, or mismatched path.
- Four outer duplicate files have a retained byte-identical peer. Directory records, nested ZIPs, and generated bytecode exclusions are described in the manifest and evidence README.
- Entry-name checks found no absolute path, parent traversal, NUL, duplicate name, or symbolic-link entry.

## Analyzer checks

The documented analyzer command produced 9 diagram observations, 21 register entries, and no parser warnings. Report schema 1.1 separates unvalidated observations from rated threat scenarios: observations have classification and evidence but no severity.

The JSON and HTML outputs reproduced byte-for-byte on a second in-memory render:

- JSON SHA-256: `35c3a2850854123b6d728c6e3dcc5165fef74ae839a5b44a710349d0240b1995`.
- HTML SHA-256: `76980e1cb5bccdcb12b0d2de10ef64edb39df98a231f24538901ff468bc19c99`.

Schema tests reject missing and unknown fields, duplicate or malformed IDs, rule IDs whose embedded threat ID disagrees with `threat_id`, invalid detector/scope combinations, and register severities that disagree with the matrix. The 21 `TM-001` through `TM-021` IDs and 11 rule IDs are stable and cross-referenced. A malicious rule title, register field, diagram title, evidence string, and parser warning were rendered as escaped text rather than markup.

Context tests confirm that a control in another section, a repeated section occurrence, another actor-initiated flow sharing a service, or a note targeting an unrelated participant does not suppress an observation. A generic `Database error` message no longer satisfies the sensitive-detail rule.

## Application checks

The clean environment ran the full suite and lint checks from the repository root:

```text
python -m pytest -q
83 passed, 1 warning

python -m ruff check .
All checks passed!

python -m ruff format --check .
47 files already formatted

python -m pip check
No broken requirements found.
```

The warning is emitted by FastAPI's current test-client compatibility module because Starlette deprecates that HTTPX bridge in favor of a future `httpx2` package. No application assertion failed.

Tests cover password hashing, generic login failures, dummy-hash verification, normalized account lockout, concurrent failure serialization, session rotation/revocation/idle/absolute expiry, CSRF on every state-changing route, CSRF session binding, server-side role checks, owner-scoped list/read/update/delete/export, owner predicates in emitted mutation SQL, AES-GCM nonce and authenticated-data behavior, SQLite plaintext absence, XSS and SQL-shaped inputs, request and field limits, quotas, pagination, safe audit fields, error responses, and cookie behavior in all three environments.

A real Uvicorn process was started with the documented command on `127.0.0.1`. Registration, login, note creation, owner read/export/delete, logout, and redirects returned the expected statuses. Cross-user read, update, and delete returned `404`; the second user's export was empty; a normal user received `403` at the admin route; a stored script-shaped title was escaped; and replay of the logged-out session redirected to login. The password and note sentinels were absent from both SQLite and server logs. Logs also contained no cookie names, master-key name, traceback, SQL statement, or local filesystem path. The process, database, and logs were removed after the check.

## Severity review

Ratings now account for verified controls and supported scope. TM-002 through TM-005 and TM-007 were reduced from High to Medium because the relevant controls are demonstrated and no bypass was found. TM-009, TM-010, TM-015, TM-017, and TM-018 were reduced to Low because the remaining scenarios are bounded, require a future regression, or concern the count-only admin surface. TM-014 was reduced to Medium because impact is limited to one local process. TM-019 and TM-020 were reduced to Medium after configuration hardening and removal of unsupported supply-chain likelihood. TM-001 remains High because valid compromised-password reuse can still cross an account boundary and MFA is not implemented.

## Optional tools

PlantUML, `pip-audit`, Bandit, and mypy were not installed, so no result is claimed for them. Ruff, Pytest, dependency consistency, the repository security review, deterministic report validation, and the real HTTP smoke test are reported separately.

## Package hygiene

Before packaging, bytecode, `__pycache__`, Pytest and Ruff caches, editable-install metadata, disposable databases, logs, temporary files, and any real `.env` file are removed. The outer ZIP is inspected independently for the same exclusions and for its required top-level folder.
